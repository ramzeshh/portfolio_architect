---
import Base from '../layouts/Base.astro';
import { getSite } from '../lib/site';
const site = getSite();
---
<Base title={site.seo.title} description={site.seo.description}>
  <h1>WIP</h1>
</Base>
